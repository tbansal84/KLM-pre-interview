Build the application using mvn clean install 

and run the jar from mabven output target folder(use java 8)

java -jar klm-travel-api-0.0.1-SNAPSHOT.jar

open the swagger URL

http://localhost:9001/swagger-ui.html#/travel-api-controller

to view and test the rest api.

With Given More time a spring MVC base UI could be created in the application.
The Metrics API could be improved for better reporting.
Code comments and cleanup is required


